{%- comment -%}
  Snippet Registry
  Purpose: Declare references for Theme Check to avoid OrphanedSnippet warnings.
  Safe at runtime: wrapped in `{% if false %}` => produces no output or side effects.
{%- endcomment -%}

{% comment %}
  {%- render 'bought-product-together' -%}
