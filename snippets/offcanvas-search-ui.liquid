{%- comment -%}
Reusable UI snippet for offcanvas search modal and SVG icons
{%- endcomment -%}

{%- if icon_type == "search" -%}
  ...existing search icon SVG...
{%- elsif icon_type == "close" -%}
  ...existing close icon SVG...
{%- endif -%}

