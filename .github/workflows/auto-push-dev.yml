name: ⚡ Auto Push DEV to Shopify

on:
  push:
    branches: [ DEV ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Shopify CLI
        run: npm install -g @shopify/cli @shopify/theme

      - name: Deploy to Shopify (DEV)
        env:
          SHOPIFY_FLAG_STORE: ${{ secrets.SHOPIFY_STORE }}
          SHOPIFY_CLI_THEME_TOKEN: ${{ secrets.SHOPIFY_CLI_THEME_TOKEN_DEV }}
          THEME_ID: ${{ secrets.SHOPIFY_THEME_ID_DEV }}
        run: |
          shopify theme push --store "\$SHOPIFY_FLAG_STORE" --theme "\$THEME_ID" --path .
