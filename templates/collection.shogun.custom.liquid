{% comment %}
Auto-generated by Shogun.
This file can be re-written at any time.
{% endcomment %}

{{ collection.description }}

{% if collection.image %}
		<img src="{{ collection.image | image_url: width:600, height:600 }}"
			 alt="Image de la collection : {{ collection.title }}"
			 role="img"
			 aria-label="Image de la collection : {{ collection.title }}"
			 width="600" height="600">
{% endif %}
